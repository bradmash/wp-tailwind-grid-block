<?php
    // $tag       = esc_html( $attributes['tagName'] ?? 'div' );
    // $aria_attr = ! empty( $attributes['ariaValue'] ) && ! empty( $attributes['ariaType'] )
    //     ? sprintf( ' %s="%s"', esc_attr( $attributes['ariaType'] ), esc_attr( $attributes['ariaValue'] ) )
    //     : '';
    // $role_attr = ! empty( $attributes['role'] ) ? ' role="' . esc_attr( $attributes['role'] ) . '"' : '';

    // return sprintf(
    //     '<%1$s%2$s%3$s>%4$s</%1$s>',
    //     $tag,
    //     $aria_attr,
    //     $role_attr,
    //     $content
    // );
?>
