<?php
    /**
     * @see https://github.com/WordPress/gutenberg/blob/trunk/docs/reference-guides/block-api/block-metadata.md#render
     */

    /**
     * Server-side rendering for the Tailwind Grid Block.
     *
     * @param array  $attributes Block attributes.
     * @param string $content    Inner blocks content.
     * @return string Rendered HTML.
     */
    //function bmashblocks_render_tailwind_grid_block( $attributes, $content ) {
        // $tag       = isset( $attributes['tagName'] ) ? $attributes['tagName'] : 'div';
        // $aria_attr = '';
        // if ( ! empty( $attributes['ariaValue'] ) && ! empty( $attributes['ariaType'] ) ) {
        //     $aria_attr = sprintf(
        //         ' %s="%s"',
        //         esc_attr( $attributes['ariaType'] ),
        //         esc_attr( $attributes['ariaValue'] )
        //     );
        // }

        // $role_attr = ! empty( $attributes['role'] )
        //     ? sprintf( ' role="%s"', esc_attr( $attributes['role'] ) )
        //     : '';

        // $columns = isset( $attributes['columns'] ) ? $attributes['columns'] : 3;
        // $gap     = isset( $attributes['gap'] ) ? $attributes['gap'] : 'gap-4';
        // $layout  = isset( $attributes['layout'] ) ? $attributes['layout'] : 'grid';

        // $layout_class = $layout === 'flex'
        //     ? 'flex'
        //     : "grid grid-cols-{$columns}";
        // $class = esc_attr( "{$layout_class} {$gap}" );

        // // %1$s = tag, %2$s = class, %3$s = aria, %4$s = role, %5$s = content
        // return sprintf(
        //     '<%1$s class="%2$s"%3$s%4$s>%5$s</%1$s>',
        //     esc_html( $tag ),
        //     $class,
        //     $aria_attr,
        //     $role_attr,
        //     $content
        // );
    //}

?>
